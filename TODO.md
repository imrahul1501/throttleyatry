# TODO - Fix all images with hd in blog page

- [ ] Inspect current `blog.html` images (done via read)
- [ ] Update `blog.html` to use high-definition / properly formatted image URLs (remove `w=1600` if needed and add/standardize `w` + `q`/`auto` parameters)
- [ ] Improve image markup for better performance: add `loading="lazy"`, remove fixed width/height if inconsistent or keep them, and add `srcset` if feasible.
- [x] Validate no broken HTML attributes.
- [ ] Run a quick local check (open file in browser) / ensure no console errors.


